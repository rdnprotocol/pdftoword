# Дараагийн алхмууд - Tesseract суулгагдсан

✅ **Tesseract OCR суулгагдсан** (`C:\Program Files\Tesseract-OCR`)
✅ **Монгол хэлний багц байна** (`mon.traineddata`)
✅ **Poppler суулгагдсан** (`C:\poppler-25.12.0\Library\bin`)
✅ **Backend суулгагдсан** (Python dependencies бүгд суулгагдсан)

## Одоо хийх зүйлс:

### 1. Frontend суулгах

```bash
setup_backend.bat
```

Эсвэл:
```bash
cd backend
pip install -r requirements.txt
```


```bash
setup_frontend.bat
```

Эсвэл:
```bash
cd frontend
npm install
```

### 2. Програмыг ажиллуулах

**Гурван терминал нээх:**

**Терминал 1:**
```bash
run_backend.bat
```

**Терминал 2:**
```bash
run_frontend.bat
```

**Терминал 3:**
```bash
run_electron.bat
```

**Эсвэл бүгдийг нэг дор:**
```bash
БҮГДИЙГ_ЭХЛҮҮЛЭХ.bat
```

## Шалгах

Tesseract зөв тохируулагдсан эсэхийг шалгах:

```bash
cd backend
python -c "import pytesseract; print(pytesseract.get_languages())"
```

Энэ нь бүх боломжтой хэлний кодуудыг харуулах ёстой. `mon` байх ёстой.

## Тэмдэглэл

- ✅ `backend/config.py` файлд Tesseract замыг тохируулсан байна
- ✅ `backend/config.py` файлд Poppler замыг тохируулсан байна
- ✅ Backend бүх dependencies суулгагдсан (Python 3.13-т тохирох хувилбарууд)
- Одоо frontend суулгах хэрэгтэй

## Дараагийн алхмууд

1. ✅ Tesseract суулгасан
2. ✅ Poppler суулгасан
3. ✅ Backend суулгасан
4. ⏳ Frontend суулгах
5. ⏳ Програмыг ажиллуулах

Дэлгэрэнгүй: **ЭХЛЭХ_ЗААВАР.md**
